# Qualification Plan G0–G7

> **STATUS: PARTIAL** — G0/G1 recovered in detail from
> `docs/ms00-run-ledger-design` (PR #285) and `swarm-e/g0-seam-graph` (PR #282);
> the G0–G7 evidence-index requirement recovered from
> `docs/architecture-gap-audit-3cff6bc` (PR #280). **Missing:** detailed exit
> criteria for G2–G7 — the full G0–G7 qualification framework existed only as
> open PR #152 ("Qualification v1 framework", review-accepted but not merged at
> audit time) in `residual-agent-harness` and was not recoverable from the
> branches available to this extraction. G2–G7 below are scaffolded strictly
> from requirements stated in recovered documents; nothing further is
> fabricated.

## Universal gate rules (normative, recovered verbatim in intent)

- No qualification gate is loosened, skipped, or bypassed.
- UNKNOWN/BLOCKED is never reported as PASS.
- Never weaken a test to obtain green; retain first-attempt failures.
- Evidence must be bound to the exact audited head SHA; prose-only claims do
  not qualify.
- Every gate produces a machine-readable evidence entry (extends the
  implementation-status manifest; see `specs/ATTESTATION-VERSIONING.md`).

## G0 — Durable ledger core + admission design and reference

- MS-00 design + reference SQLite implementation + contract-test skeletons
  green against the reference (contract suite: CAS, exactly-one-terminal,
  crash-recovery, reconciliation, fencing).
- Seam DAG checked and import-layer violations resolved or explicitly exempted
  (1 known violation at base: `runtime_journal → worker_contract` upward
  import).
- Exit (G0-impl): production MS-00 ledger bound to the contract suite with
  **zero test edits**; KP-01..08 and KP-15..17 green.

## G1 — Reconciliation and writer service

- MS-01 reconciliation against G0 ledgers, reproducing Station `recover`
  semantics: KP-11..13, KP-18 green; projection determinism proof;
  `control.reconciled` evidence.
- MS-08 writer service (single-writer lease, durable fencing tokens) fronting
  the ledger: writer-lease CAS under multi-process contention; stale-fencing
  rejection; KP-14 watchdog-ablation outcome diff = [].

## G2 — Production-control closure on the ledger (scaffolded)

Recovered requirement (gap audit Tier 2): rewire restart terminal-state
preservation and no-duplicate-external-action onto the ledger; requalify the
retained FAIL cells (#207 STRESS-B1, #212 missing-usage accounting-before-
authority) without weakening tests; schema migration strategy. Detailed exit
criteria: **MISSING** (PR #152 not recoverable).

## G3 — Provider contract closure (scaffolded)

Recovered requirement: MS-01 provider-resolver canonical contract landed
(findings F-1..F-4 closed: kind↔host pinning, parse-time dispatchability,
credential-clear totality, frozen alias semantics); real-provider E2E evidence
bound to exact head. Detailed exit criteria: **MISSING**.

## G4 — Recovery and resilience evidence (scaffolded)

Recovered requirement: integrated recovery suite against the reconciled ledger;
host-loss / recovery evidence retained; disaster/restart matrix green at exact
head. Detailed exit criteria: **MISSING**.

## G5 — Soak and determinism classification (scaffolded)

Recovered requirement: D0–D4 determinism classes defined and applied;
24h/72h wall-clock soak executed with a real executor (deterministic simulator
results do not qualify); orchestration-overhead measurement. Detailed exit
criteria: **MISSING**.

## G6 — Observability and security lane closure (scaffolded)

Recovered requirement: SLO compliance evidence (not just definitions); bounded-
cardinality metrics; telemetry-failure isolation from verifier semantics;
secret-leakage scan in CI; health-endpoint separation; bounded queues/
backpressure; graceful drain. Detailed exit criteria: **MISSING**.

## G7 — Release candidate freeze (scaffolded)

Recovered requirement: frozen RC lane with all exit criteria checked; exact-head
CI PASS at the RC commit; artifact freeze (wheel bound to commit/tree);
maintainer attestation. Depends on G0–G6. Detailed exit criteria: **MISSING**.

## Recovery action

When PR #152 (or successor) lands on `residual-agent-harness` main, extract its
G0–G7 definitions into this file as an editorial-class amendment; any
divergence from the scaffolds above is a gate-change amendment under
`GOVERNANCE.md`.
